<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp"
    style="@style/AppTheme"
    android:background="@drawable/background">

    <!-- Текст для текущего действия -->
    <TextView
        android:id="@+id/actionTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Готовы начать дыхательную практику?"
        android:textSize="24sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="16dp" />

    <!-- Таймер -->
    <TextView
        android:id="@+id/timerTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0"
        android:textSize="48sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="24dp" />

    <!-- Кастомное View для квадрата -->
    <com.example.application02.tecnics.SquareView
        android:id="@+id/squareView"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_marginBottom="24dp" />

    <!-- Кнопка для управления практикой -->
    <Button
        android:id="@+id/toggleButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Начать"
        android:padding="12dp"
        android:textSize="18sp" />

    <!-- Кнопка для показа инструкции -->
    <Button
        android:id="@+id/instructionButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Инструкция"
        android:padding="12dp"
        android:textSize="18sp"
        android:layout_marginTop="24dp" />
</LinearLayout>